# Setup HTTP port 80 on Istio Ingress Gateway for "test-local" environment/namespace
- op: add
  path: /spec/servers/-
  value:
    port:
      number: 80
      name: http
      protocol: HTTP
    hosts:
    - istio.test-local.devops.net

